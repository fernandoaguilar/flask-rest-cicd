version: 0.2

run-as: ubuntu

# env:
#   variables:
#     key: "value"
#     key: "value"
#   parameter-store:
#     key: "value"
#     key: "value"

phases:
  install:
    run-as: ubuntu
    runtime-versions:
      python: 3.6
      # runtime: version
    commands:
      - pip install -r requirements.txt
  build:
    run-as: ubuntu
    commands:
      - tox -v
